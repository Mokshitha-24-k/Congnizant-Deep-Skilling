Experiment 1: Git Configuration and Repository Setup
Aim

To configure Git, initialize a local repository, create a file, commit it, and push it to GitHub.

Commands Used
git --version
git config --global user.name "Mokshitha"
git config --global user.email "nagasaimokshithak@email.com"
git config --list
mkdir GitDemo
cd GitDemo
git init
echo Welcome > welcome.txt
git status
git add welcome.txt
git commit -m "Initial commit"
git remote add origin https://github.com/<Mokshitha-24-k>/GitDemo.git
git branch -M main
git push -u origin main
Result

Git was configured successfully. A local repository was created and pushed to GitHub.

(Insert screenshots of Git Bash and GitHub repository.)

Experiment 2: Using .gitignore
Aim

To ignore unwanted files and folders.

Commands Used
mkdir log
echo Error > test.log
echo *.log > .gitignore
echo log/ >> .gitignore
git status
git add .gitignore
git commit -m "Added gitignore"
git push
Result

The .log files and log folder were ignored successfully.

(Insert screenshots.)

Experiment 3: Branching and Merging
Aim

To create a branch, commit changes, merge with main, and delete the branch.

Commands Used
git checkout -b GitNewBranch
echo Branch > branch.txt
git add .
git commit -m "Added branch file"
git checkout main
git merge GitNewBranch
git branch -d GitNewBranch
git push
Result

The branch was created, merged successfully, and deleted after merging.

(Insert screenshots of git branch, git log, and GitHub commits.)

Experiment 4: Merge Conflict Resolution
Aim

To resolve merge conflicts.

Commands Used
git checkout -b GitWork
echo Branch Content > hello.xml
git add .
git commit -m "Branch changes"

git checkout main
echo Main Content > hello.xml
git add .
git commit -m "Main changes"

git merge GitWork

Resolve the conflict manually.

git add hello.xml
git commit -m "Resolved merge conflict"
git push
Result

The merge conflict was resolved successfully, and the changes were pushed to GitHub.

(Insert screenshots of conflict markers and resolved file.)

Experiment 5: Cleanup and Push
Aim

To synchronize the local and remote repositories.

Commands Used
git status
git branch
git pull origin main
git push origin main
git log --oneline --graph --decorate